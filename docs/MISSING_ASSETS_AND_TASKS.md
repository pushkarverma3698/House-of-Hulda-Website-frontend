# House of Hulda — Missing Assets & Tasks

## Phase 3: The Apple-Tier Cinematic Pivot (Active)
- [x] Tear down R3F `DisplacementPlane` and faux-3D components.
- [x] Implement GSAP `ScrollTrigger` and Lenis smooth scrolling.
- [x] Build `CinematicScrubber.tsx` for HTML5 video/image sequence canvas scrubbing.
- [x] Choreograph UI elements (Act 1-9) to fade in/out perfectly with scroll progress using GSAP.

## Required Assets (The True 2026 Shift)
**The cinematic pivot requires a new asset pipeline.**
We no longer need static Midjourney JPEGs and depth maps. We need:
1. **The Hero Sequence**: A single, continuous AI-generated (Sora / Runway Gen-3) cinematic video file moving through the environments (valley -> house -> hearth).
   - *Current Status:* Using `mist_loop.mp4` as a placeholder to prove the GSAP scrubber.
   - *Requirement:* MP4, 1080p or 4K, locked to 30fps or 60fps, visually spanning the narrative beats.
